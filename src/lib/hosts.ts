/**
 * 플랫폼 호스트 이름 상수.
 *
 * 권위 있는 값은 서버가 준다 — 로그인 이후 화면은 `/meta/request-options`의
 * `sshHost`나 VM의 `sshHost`를 쓰고, 이 상수를 쓰지 않는다. 상수가 필요한 곳은
 * **API를 부를 수 없는 화면**(비인증 랜딩)과, 응답이 아직 없을 때의 fallback뿐이다.
 *
 * 그래서 이 값은 서버 설정과 이중 관리될 수밖에 없다. 흩어 두면 도메인이 바뀔 때
 * 일부만 고쳐진 채 남으므로(실제로 랜딩 두 곳이 API를 아예 보지 않고 옛 호스트를
 * 그대로 표시하고 있었다) 한 곳에 모아 둔다.
 */
export const SSH_GATEWAY_HOST = 'ssh.pcl.kr'
